#include <stdio.h>
#include <string.h>
#include <stdbool.h>

static const char* dotName = "Sum operator";
static const char* commaName = "Sum operator";
static const char* semiColonName = "Sum operator";

bool punctuation(char* name, char* token) {
  return false;
}
